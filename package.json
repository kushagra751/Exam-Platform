{
  "name": "exam-platform-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace client",
    "server": "npm run dev --workspace server",
    "client": "npm run dev --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
